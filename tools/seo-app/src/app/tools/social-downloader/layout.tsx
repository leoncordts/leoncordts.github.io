import type { ReactNode } from 'react';

export const metadata = {
  title: 'Social Media Downloader | Leon Cordts IT Solutions',
  description: 'Videos von TikTok, Instagram, X (Twitter) und Facebook kostenlos herunterladen — ohne Wasserzeichen.',
};

export default function SocialDownloaderLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
